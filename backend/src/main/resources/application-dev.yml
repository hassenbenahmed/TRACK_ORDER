server:
  port: ${SERVER_PORT:8080}

spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  output:
    ansi:
      enabled: always

logging:
  level:
    root: DEBUG
    com.trackorder: DEBUG
