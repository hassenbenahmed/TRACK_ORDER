apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    spec:
      containers:
      - name: trackorder-backend
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: dev
